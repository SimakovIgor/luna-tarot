/**
 * Глобальные константы фронта. Если что-то перейдёт в env — поднимем сюда же.
 */

/** Имя бота в Telegram (без @). */
export const BOT_USERNAME = 'luna_taro_card_bot';

/** Публичная ссылка на бота — для кнопок «открой и ты» в share-flow. */
export const BOT_URL = `https://t.me/${BOT_USERNAME}`;
